global:
  scrape_interval: 15s
  evaluation_interval: 15s
  external_labels:
    cluster: 'funstuffbarn'
    environment: 'production'

alerting:
  alertmanagers:
    - static_configs:
        - targets: []

rule_files: []

scrape_configs:
  - job_name: 'funstuffbarn-agent'
    static_configs:
      - targets: ['host.docker.internal:8080']
        labels:
          service: 'agent-etsy'
          environment: 'production'
    metrics_path: '/metrics'
    scrape_interval: 10s

  - job_name: 'prometheus'
    static_configs:
      - targets: ['localhost:9090']

  - job_name: 'node-exporter'
    static_configs:
      - targets: ['host.docker.internal:9100']